# Master Key 说明

## 什么是 Master Key?

Master Key (主密码) 用于加密 / 解密 `.env` 文件中的所有敏感配置。

**不是一个文件**, 而是你在运行以下脚本时自行设置的密码:

- `scripts/encrypt.bat` (Windows) / `scripts/encrypt-secrets.sh` (macOS/Linux)
- `scripts/decrypt.bat` (Windows) / `scripts/decrypt-secrets.sh` (macOS/Linux)

## 如何工作?

```
.env (明文) --[加密]--> secrets/.env.encrypted (密文)
                 |
                 v
           Master Password (你记在脑中的密码)

secrets/.env.encrypted --[解密]--> .env (明文)
                 |
                 v
           Master Password (同一个密码)
```

## 加密算法

- **AES-256-CBC** — 银行级加密
- **PBKDF2** 密钥派生, 100000 次迭代
- **OpenSSL** 实现, 跨平台兼容

## 最佳实践

1. **使用强密码**: 至少12位, 包含大小写字母、数字、特殊符号
2. **牢记密码**: 丢失密码 = 丢失所有配置, 无法恢复
3. **不要写在文件中**: 密码只存在于你的记忆中
4. **定期备份**: 加密文件 `secrets/.env.encrypted` 会被备份脚本自动包含

## 使用示例

### 首次加密
```
# Windows
scripts\encrypt.bat

# macOS / Linux
bash scripts/encrypt-secrets.sh
```

### 解密 (临时使用)
```
# Windows
scripts\decrypt.bat

# macOS / Linux
bash scripts/decrypt-secrets.sh
```

### 启动时自动解密
`start.bat` / `start.sh` 脚本会自动检测加密文件并提示输入密码,
启动后自动清理明文 `.env` 文件。
